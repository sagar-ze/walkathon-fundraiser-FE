import { Grid } from '@mui/material';

const Dashboard = () => {
  return (
    <Grid container>
      <Grid item xs={12}>
        Welcome to the main page
      </Grid>
    </Grid>
  );
};

export default Dashboard;
